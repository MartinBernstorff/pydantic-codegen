# pydantic-codegen

_Updated 2026-08-28_

```console
$ pip install pydantic-codegen
```

Generates Pydantic models so you never again ship a `class Usre(BaseModel)` to
production and find out from a customer.

Development conventions, task list and release process live in [AGENTS.md](AGENTS.md).
